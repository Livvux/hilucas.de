---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Lucas Kleipödszus",
      url: "https://hilucas.de/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Lucas Kleipödszus",
      logo: {
        "@type": "ImageObject",
        url: "https://hilucas.de/livvux-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://hilucas.de/livvux-avatar.jpg",
    articleSection: data.tags?.[0] || "Technologie",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Lucas Kleipödszus",
    url: "https://hilucas.de",
    image: "https://hilucas.de/livvux-avatar.jpg",
    sameAs: [
      "https://github.com/Livvux",
      "https://x.com/Livvux",
      "https://bsky.app/profile/livvux.com",
    ],
    jobTitle: "Software Engineer",
    description:
      "Fullstack Developer aus Deutschland. Spezialist für Webdesign, SEO und innovative digitale Lösungen.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Lucas Kleipödszus",
    url: "https://hilucas.de",
    description:
      "Fullstack Developer aus Deutschland. Spezialist für Webdesign, SEO und innovative digitale Lösungen.",
    author: {
      "@type": "Person",
      name: "Lucas Kleipödszus",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://hilucas.de/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />